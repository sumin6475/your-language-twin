# Phase 0 spike deps (throwaway — not the product's dependency set).
# CPU-only; no GPU needed.
#
# NOTE: loosened to >= because this machine runs Python 3.14 (2026), which is newer
# than the exact pins originally chosen — those older versions have no 3.14 wheels.
# For a spike, "installs and runs on this machine" beats reproducible pins. The real
# product (Phase 1, its own venv/lockfile) can pin exactly.

sentence-transformers>=3.3    # loads mstyledistance / styledistance style embedders
torch>=2.9                    # CPU wheel; pulled in by sentence-transformers
numpy>=2.1                    # vectors + cosine k-NN
datasets>=3.2                 # ready-made HF transcript datasets for the corpus
groq>=0.13                    # Groq Whisper ASR (free tier)
openai>=1.50                  # style-preserving KO→EN translation (gpt-4o-mini); Path B improvement
huggingface-hub>=0.27         # model/dataset download + auth if a gated dataset needs it
